{
  "name": "book-api",
  "version": "1.0.0",
  "description": "REST API for managing books",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}